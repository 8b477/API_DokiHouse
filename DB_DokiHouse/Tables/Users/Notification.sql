CREATE TABLE [dbo].[Notification]
(
	[Id] INT NOT NULL PRIMARY KEY IDENTITY,
	[Sms] BIT DEFAULT 0 NOT NULL,
	[Mail] BIT DEFAULT 0 NOT NULL,
	[IdUser] INT NOT NULL
	FOREIGN KEY (IdUser) REFERENCES [User](Id)
)
